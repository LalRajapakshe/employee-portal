# Frontend / BFF
NEXT_PUBLIC_APP_NAME=Employee Portal
CORE_API_BASE_URL=http://localhost:5000
SESSION_COOKIE_NAME=employee_portal_session

# Database / backend reference
PORTAL_DB_CONNECTION=Server=localhost;Database=EmployeePortal;Trusted_Connection=True;TrustServerCertificate=True;
PAYROLL_DB_CONNECTION=Server=localhost;Database=Payroll;Trusted_Connection=True;TrustServerCertificate=True;

# Demo fallback user for local testing before login flow is wired
DEMO_PORTAL_USER=demo.user